const gam=g=>g==="AA"?["A","A"]:g==="aa"?["a","a"]:["A","a"];const combine=(a,b)=>[a,b].sort((x,y)=>x==="A"?-1:1).join("");let correct=25;function buildGrid(){const x=gam(p1.value),y=gam(p2.value);const r=[combine(x[0],y[0]),combine(x[1],y[0]),combine(x[0],y[1]),combine(x[1],y[1])];x1.textContent=x[0];x2.textContent=x[1];y1.textContent=y[0];y2.textContent=y[1];[c11,c12,c21,c22].forEach((c,i)=>c.textContent=r[i]);correct=r.filter(v=>v==="aa").length*25;percent.textContent=correct+"%";result.textContent="";}document.querySelector("#build").addEventListener("click",buildGrid);document.querySelector("#check").addEventListener("click",()=>result.textContent=Number(answer.value)===correct?"Верно.":"Неверно. Правильный ответ: "+correct+"%.");buildGrid();